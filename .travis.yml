language: java
jdk:
  oraclejdk8
cache:
  directories: $HOME/.gradle

script:
   ./gradlew distTar
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
after_script:
  - git add .
  # something@example.com
  - git config user.email "whisrael@gmail.com"
  # First Last
  - git config user.name "annoymousGiraf"
  - git commit -m "updating outputs"
 
  - git push -f "https://$GITHUB_TOKEN@github.com/annoymousGiraf/health-checker" gh-pages > /dev/null 2>&1
